# MEBODY TODO

기준: 2026-09-22. 운영 DB 실측과 실제 코드로 상태를 다시 맞췄다.
적용 상태는 `npm run verify:migrations` 가 판정한다. 지금은 미적용 0건·잔재 0건.

---

## P0. 사람이 해야 하는 것

- [ ] **법적 문서 빈칸 채우기** — 노란 표시로 본문에 드러나 있다.
      남은 것: 주소, 문의 이메일, 전화번호, 개인정보 보호책임자 성명·직책.
      시행일은 2026-07-22 로 넣었고, 법인이 아니라 "(주)" 표기를 빼고 운영자 최우혁으로 적었다.
      사업자등록번호와 통신판매업 신고번호는 "사업자등록 준비 중" 으로 표시해 두었다
- [ ] **사업자등록·통신판매업 신고** — 앱에 상품 15개와 마켓 탭이 있다.
      결제를 켜기 전에 정리해야 한다. 신고 없이 파는 것 자체가 문제가 된다
- [ ] **법무 검토** — 지금 문서는 검토받을 초안이다. 검토 없이 배포하면 안 된다
- [x] `db/journey/052`·`053` 적용 (전문가 확장 기반)
- [x] `db/journey/054` 적용 (이벤트 수집. 검증 12건 통과)
- [x] `db/journey/055` 적용 (가입 동의 시각)
- [ ] **Supabase 자체 SMTP 연결** (Authentication → Emails. Resend·SES 등)
      2026-09-21 부터 이메일 가입에 확인 메일이 붙었다. 그런데 Supabase **기본 SMTP 는 시간당
      몇 통**이라 실측에서 연속 두어 번 만에 `429` 로 가입이 실패했다. 자체 SMTP 를 붙이기 전까지
      외부 가입자를 받으면 가입되는 사람과 안 되는 사람이 갈린다.
      급하면 `MEBODY_AUTH_REQUIRE_EMAIL_VERIFICATION=false` 로 되돌릴 수 있다
- [ ] **Supabase Redirect URLs 에 앱 주소 등록** — 확인 메일 링크가 `mebody.app-url` 로 돌아온다.
      허용 목록에 없으면 링크가 동작하지 않는다
- [ ] **AdMob 보상형 광고 단위 발급** → `.env.local` 의 `VITE_ADMOB_REWARDED`.
      배너는 `…/8555895404` 로 적용 완료. 보상형이 비어 있으면 그 자리에 구글 테스트 광고가 나간다.
      `npm run ads:check` 가 상태를 보여준다
- [ ] **Railway 재배포** — 지금 `mebody-server-production.up.railway.app` 은 전 경로 404 다.
      스프링이 아니라 Railway 엣지가 내는 `Application not found` — 그 주소에 배포본이 없다.
      앱의 `VITE_API_BASE_URL` 이 이 주소를 가리키므로, 배포 전까지 결제·주문·휴대폰 가입·탈퇴가 잠긴다
- [ ] Kakao Developers 앱 생성 → JavaScript 키 → 플랫폼 Web 도메인 등록 → 카카오톡 공유 활성화
- [ ] `VITE_KAKAO_JAVASCRIPT_KEY` 를 `.env.local` 과 Vercel 에 설정

## P0. 운영 전 필수 (코드)

- [x] **결과 설명 강화** — 축 게이지 아래에 내 답변 근거를 붙였다. 점수 로직은 건드리지 않았다
- [x] **최소 이벤트 수집기** — `analytics_events` 에 실제로 남긴다 (`054`)
- [x] 회원가입 약관·개인정보 동의 저장 시각 (`055`)
- [ ] **14일 루틴 알림** — 네이티브 작업이라 마지막으로 미룸.
      `@capacitor/local-notifications` 추가, 안드로이드 13+ 권한, 재빌드 필요
- [ ] 실제 결제사 확정 (Toss 또는 Google Play Billing). 지금은 dev 어댑터.
      **`CheckoutScreen.purchaseTokenFor()` 가 만드는 문자열은 결제 증명이 아니다.**
      앱이 스스로 만든 값을 서버가 믿으므로, 이 경로를 실사용자에게 열면 돈 없이 유료가 켜진다.
      사업자 등록 후 Play Billing 을 붙일 때 통째로 걷어낸다 (RR-09)
- [ ] 결제 webhook 검증

## P1. 전문가 확장

`docs/MEBODY_PROFESSIONAL_ROADMAP.md` 에 단계별 가설·지표·실패 조건이 있다.

- [x] Phase 0 — 전문가 테이블 2개, RLS 헬퍼, 고객 결과 열람 함수 (`052`·`053`, 검증 24건)
- [x] Phase 1 — 전문가 계정 발급, 초대 링크, 고객 결과 한 화면 (서버 콘솔 「고객 관리」 탭)
- [x] Phase 1 — 앱의 `?invite=<token>` 동의 화면 한 장 + 마이페이지 「연결된 전문가」 해지
      검증 `npm run verify:professional-api` 38건 (DB 24건과 별개)
- [x] **운영 지표 화면** (`068`) — 콘솔 「지표」 탭. 네 퍼널 + 주간 활성 전문가.
      앱 이벤트 8개 추가(저니·수익 퍼널). 검증 25건
- [x] **DB 잔재 정리** (`061`·`062`) — 테이블 40 → 35개.
      다만 정리하다 기존 기능 3개를 깼고 전부 고쳤다(`064`·`065`·`067`).
      **테이블을 지울 때는 코드가 아니라 DB 함수까지 봐야 한다** — 그 검사를 verify:migrations 에 넣음
- [x] 홈페이지 「미션 진행」 카드를 실제 데이터로 (늘 0% 였다 → 69.7% · DAY 별 진행)
- [x] **적립금 전면 개편** (`057`·`058`) — 평균 482원/월 → 42원, 하드 상한 49원/월.
      주사위 눈(1~6 균등)과 금액을 분리해 "줄 수 없는 금액을 암시하지 않는다".
      상한은 청구 함수가 아니라 user_rewards 트리거로 보증. 검증 20건
- [x] Phase 2 — 고객 수행 데이터 열람 (`056`, `get_client_journey_summary()`)
      진행률·일자별 타임라인·피드백. 동의 범위도 같이 넓혀 문구에 반영. 검증 48건
      주간 활성 전문가용 `professional_activity_log` 포함 (로드맵의 "테이블 추가 없음" 은 틀렸다)
- [x] Phase 3 — 미션 배정·메모 (`059`·`060`). 라이브러리에서만 고르기 · 하루 3개 · 메모 200자.
      적립금은 대상이되 057 의 월 상한(49원)이 구조적으로 막는다. 검증 61건
- [ ] Phase 4 — 규칙 엔진 초안을 전문가 콘솔에 보여주고 편집하게 한다 (AI 없이)
- [ ] Phase 5 — 주의 대시보드. 새 데이터 없이 집계 쿼리로 가능

## P1. 콘텐츠

- [ ] 동작 이미지 23종(이완·스트레칭)을 Storage `actions/` 에 올린다
- [ ] 16개 코드별 15분 루틴 데이터를 확정한다
- [ ] 적립금 사용처를 정한다. 지금은 적립만 되고 쓸 곳이 없다
- [ ] 부위별 콘텐츠 15종(등·허리·무릎·발목·발바닥). 4축 콘텐츠 정착 후

## P2. 제품 완성도

- [ ] 마이페이지에 과거 결과 리스트와 상세 진입
- [ ] 결과 다운로드 PNG 검토
- [ ] 유튜브·상품 mock 영역을 운영 콘텐츠 관리 방식으로
- [ ] 코드별 공유 이미지(1200×630). 지금은 공용 `og-image.png` 하나

## P3. 기술

- [ ] 결과·코드 플랜·마이페이지 route-level lazy loading
- [ ] 이미지 WebP 변환과 preload 정책
- [ ] `prompts`(13행)·`sere_contents`(3행) 정리 검토. 앱·서버 참조 0곳
- [ ] `body_bti_results`·`user_mission_progress` 정리 검토. 0행, 서버 1곳만 참조
- [ ] 네이티브 안드로이드 재검증 (뒤로가기·프로세스 종료·오프라인 재실행). 사업자 등록 후 Play Console 내부 테스트
- [ ] 배포 (Vercel · Railway). **정말 마지막에**

---

## 완료

### 2026-09 (이번 회차)

- [x] 서버 상품 등록 — 사진 필수 (`039`, 검증 16 + HTTP 32)
- [x] 결제 원장·주소·주문 (`040`), 권한 구멍 수정 (`041`, 검증 50)
- [x] 배송 상태·주문 취소·광고 SSV (`042`, 검증 38 + HTTP 24)
- [x] 옛 문항 정리 (`043`·`049`·`050`) — 32문항만 남김
- [x] **진단 응답 열람 차단** (`044`) — 적용 전 399행 중 381행이 id 없이 읽혔다
- [x] **제출된 결과 잠금** (`045`) — id 를 알아도 덮어쓸 수 없다
- [x] **계정 탈퇴** (`046`, 검증 24 + HTTP 17) — 거래 기록은 남기고 사람만 끊는다
- [x] `create_order` 중복 함수 제거 (`047`) — 주소 없이 주문하면 실패하던 문제
- [x] `app_error_log` 권한 정리 (`048`) — TRUNCATE 가 RLS 를 우회했다
- [x] 옛 진단 응답 352건 정리 (`051`)
- [x] 전문가 확장 기반 (`052`·`053`, 검증 24)
- [x] 이벤트 수집 (`054`, 검증 12) — 개인 식별 값 없이 퍼널만
- [x] 가입 동의 시각 기록 (`055`)
- [x] 결과 화면에 "왜 이 코드인지" 근거 표시
- [x] 약관 시행일·운영자 표기 정정 (법인 아님)
- [x] 결과 공유 — 카카오·OS 공유·링크 복사. 링크에 result id 를 넣지 않는다
- [x] 회원가입 — 이메일·휴대폰. 2026-09-21 부터 **이메일은 확인 메일 ON**, 휴대폰은 즉시 가입
      (휴대폰은 SMS 제공자가 없어 인증 수단 자체가 없음)
- [x] 휴대폰 계정 복구용 이메일, 번호 로그인, 재설정
- [x] 마이페이지 불러오기 실패를 "데이터 없음" 과 구분
- [x] 법적 문서 본문 교체 (빈칸은 표시)
- [x] 문항 선택지 호버 가독성 (앱·`/sample` 둘 다)
- [x] 진단 소개 축 게이지가 진입 즉시 움직이게
- [x] 문항 전환 시 스크롤 위치 초기화
- [x] 마이그레이션 적용 상태 점검 스크립트 (`verify:migrations`)
- [x] 전문가 확장 설계 문서 4종 (`docs/MEBODY_*`)
- [x] 콘솔 링크를 판매자·전문가에게도 노출 + 역할별 시작 탭 (전에는 /admin 을 직접 쳐야 했다)
- [x] 푸터의 「(주)mebody」 제거 — 법인이 없어 약관·처리방침과 어긋났다
- [x] **전문가 Phase 1** — 초대·동의·결과 열람. 동의 전에는 이름도 코드도 내려주지 않고,
      고객이 언제든 거둘 수 있다. 검증 38건
- [x] **결과 화면이 하얗게 죽던 버그** — `HomeScreen` 의 `result_viewed` 훅이 조기 return
      뒤에 있어 "Rendered more hooks than during the previous render" 로 터졌다
- [x] `UserRole` 에 PROFESSIONAL 누락 — 052 가 DB 에만 더해서 전문가 계정은 로그인만 해도 500
- [x] `/me` 로 직접 들어오면 역할 메뉴(판매자·전문가·관리자)가 전부 숨던 문제
- [x] `.env.development.local` 분리 — dev 는 localhost, 빌드는 배포 주소
- [x] **공개 승인 엔드포인트 우회 차단** — 확인 절차를 켜도 휴대폰 식별자는 검사를 건너뛰어
      그냥 승인됐다. 계정 존재 여부도 응답으로 새고 있었다. 둘 다 닫음 (`verify:approve-guard`)
- [x] 이메일 확인 절차 ON + 확인 대기 계정도 동의 시각을 남기도록 수정
      (안 그러면 auth.users 에 사람은 생기고 동의 증적은 없는 상태가 된다)
- [x] APK 에 `localhost` API base 가 박히는 사고 차단 (`env:check` 가 `app:build`·`app:apk` 를 막는다)
- [x] AdMob 하단 배너 단위 적용 + 진단용 `/api/public/health`
- [x] 출시 준비도 문서에 해제 현황 반영. RR-03(안전 중단 응답)은 범위에서 제외

### 그 이전

- [x] 32문항 구조 (A10 + B6 + C9 + D7)와 4축 코드 계산
- [x] Supabase 문항·응답·결과 콘텐츠 연결
- [x] 14일 저니 — 템플릿, 미션 배정 규칙, 피드백, 리포트, 재측정 비교
- [x] 적립금 원장과 지급 규칙
- [x] 즉시 액션 콘텐츠 3종 연결
- [x] 비회원 결과 기억을 현재 탭 세션으로 제한
- [x] PWA 캐싱 제거와 과거 service worker 정리
